I want to try to train a model on dummy images. Two classes, one is a little brighter than the other. Should be easy to classify.

It ended up working. Under the right hyperparameters, the model was able to classify correctly.